import "./BlogStyles.css";

export default function Content() {
  return (
    <div>
      <p>
        If you've pushed code to your own git repos in the past, you probably only had one single branch, called master or main.
      </p>
    </div>
  );
}
